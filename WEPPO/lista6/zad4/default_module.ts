function one1(n: number) {
    console.log(`I am first module: ${n}`);
}

const somestr: string = 'I am some string!'

export default function one2(n: number) {
    console.log(`I am second module: ${n}`);
}